services:
  proxy:
    image: ginuerzh/gost:latest
    container_name: twitter-proxy
    restart: unless-stopped
    ports:
      - "8388:8388"
    command: "-L http://scraper:${PROXY_PASSWORD}@:8388"
    environment:
      - PROXY_PASSWORD=${PROXY_PASSWORD}
