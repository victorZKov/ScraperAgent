# TEMPLATE — replace <CLOUDFLARE_TUNNEL_TOKEN> before applying
# Do NOT commit the real token to git
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-tunnel-token
  namespace: shared
type: Opaque
stringData:
  token: "<CLOUDFLARE_TUNNEL_TOKEN>"
