# TEMPLATE — replace all <PLACEHOLDERS> before applying
# Do NOT commit real credentials to git
apiVersion: v1
kind: Secret
metadata:
  name: scraperagent-ui-secrets
  namespace: scraperagent
type: Opaque
stringData:
  NEXT_PUBLIC_OIDC_AUTHORITY: "https://auth.quantumapi.eu"
  NEXT_PUBLIC_OIDC_CLIENT_ID: "kovimatic_scraperagent-ui"
  OIDC_CLIENT_SECRET: "<QUANTUMID_CLIENT_SECRET>"
  NEXTAUTH_SECRET: "<NEXTAUTH_SECRET>"
  NEXTAUTH_URL: "https://scraperagent.eu"
